# acf-api-manager-docker-demo
Demo to illustrate how to configure Adobe Coldfusion API manager with Docker Compose

**Note: If you don't get log files created under the `/logs` folder, check the permissions on that folder. They need to be `777` on *nix or "Full Control" for all users in Windows.
